package dev.kinero.kinerobackend.training.model;
public enum ProgramType {
    PPL, // Push Pull Legs
    UPPER_LOWER,
    FULL_BODY,
    BODY_PART_SPLIT,
    PUSH_PULL_LEGS_UPPER_LOWER, // 6 days
    CUSTOM
}
